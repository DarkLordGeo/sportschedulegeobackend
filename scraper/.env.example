# Use the same Neon PostgreSQL database as the production backend.
DATABASE_URL=

# Required because the scraper imports Django settings.
DJANGO_SETTINGS_MODULE=config.settings
SECRET_KEY=
DEBUG=False

# Optional, keeps long-running database connections healthy.
DATABASE_CONN_MAX_AGE=600
